#ifndef SECRETS_H
#define SECRETS_H

// WiFi Configuration
// Copy this file to secrets.h and fill in your actual credentials
const char* WIFI_SSID = "YOUR_WIFI_SSID";
const char* WIFI_PASSWORD = "YOUR_WIFI_PASSWORD";

// MQTT Configuration
const char* MQTT_BROKER_IP = "192.168.1.100";  // Your MQTT broker IP address
const char* MQTT_USERNAME = "";  // Leave empty if not required
const char* MQTT_PASSWORD = "";  // Leave empty if not required

#endif
